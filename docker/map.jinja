{% set docker = salt['grains.filter_by']({
    'Debian': {
      'docker-version': '1.7.1',
      'docker-py-version': '1.3.0',
      'docker-compose-version': '1.3.3',
    },
    'default': 'Debian',
}, merge=salt['pillar.get']('docker',{})) %}
